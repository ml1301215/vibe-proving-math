# vibe_proving dependencies
# Install from the repository root:
#   python -m pip install -r requirements.txt

# Web API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.6

# LLM and HTTP clients
openai>=1.30.0
httpx>=0.27.0

# Formalization / Harmonic Aristotle
aristotlelib>=1.0.1

# PDF and document parsing
PyMuPDF>=1.24.0
ftfy>=6.1.0
# Optional, heavy local PDF parser fallback. The default PDF path uses Nanonets/MinerU.
# Install manually only if you want local Docling fallback:
#   python -m pip install "docling>=2.0.0"

# HTML parsing for MacTutor search
beautifulsoup4>=4.12.0
